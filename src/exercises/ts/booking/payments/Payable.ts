
export default interface Payable {
  // declare an interface method named 'getRate()'
  // the method accepts no parameters
  // the method has a 'number' return-type

  // declare an interface method named 'getCurrencyCode()'
  // the method accepts no parameters
  // the method has a 'string' return-type
}
